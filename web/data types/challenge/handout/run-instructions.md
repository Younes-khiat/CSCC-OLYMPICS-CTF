# Run Instructions

## Requirements

- Java 26 is currently installed in this environment.
- Use the Gradle wrapper from the project root.

## Start the challenge

```powershell
Set-Location c:\work\demo
.\gradlew bootRun
```

Then open `http://localhost:8080`.
