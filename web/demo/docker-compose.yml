version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    # For a CTF it's generally good to set limits
    mem_limit: 512m
    cpus: "0.5"
